name: Deploy Production

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    uses: marcel-tuinstra/devops/.github/workflows/reusable-cd-nuxt-ssg.yml@v1
    secrets: inherit
    with:
      service-name: site-tuinstra
      image-name: ghcr.io/marcel-tuinstra/site-tuinstra
      environment: production
      remote-path: /mnt/ssd1000-01/projects/site-tuinstra/production
      ssh-user: mtuinstra
      dockerfile: Dockerfile
      compose-file: docker-compose.production.yml
