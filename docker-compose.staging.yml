services:
  site-tuinstra:
    image: ghcr.io/marcel-tuinstra/site-tuinstra:latest
    ports:
      - "3101:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80/health"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
